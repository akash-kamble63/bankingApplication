server:
  port: 8090

spring:
  application:
    name: user-service

  datasource:
    url: jdbc:postgresql://localhost:5432/userdb
    username: postgres
    password: admin
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

app:
  config:
    keycloak:
      server-url: http://localhost:8080
      realm: banking-app
      client-id: user-service
      client-secret: 41CzX4dw1NqwfLnZ8mp1i7PT91yyPQeh

  codes:
    bad_request: 400
    conflict: 409
    success: 200
    not_found: 404
